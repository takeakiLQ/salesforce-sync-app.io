# react-slider@2.0.6 の peerDependencies が react ^16 || ^17 || ^18 のままで、
# 本プロジェクトの React 19 と衝突するため、npm install が ERESOLVE で失敗する。
# 実動作に問題は出ていないため、依存解決を従来方式にしてインストールを通す。
#
# react-slider が React 19 に対応したら（または別のスライダーに置き換えたら）
# この設定は削除してよい。使用箇所は AvailabilityPage の時間帯スライダーのみ。
legacy-peer-deps=true
